import styled from "@emotion/styled";

export const LabelCurrency = styled.label`
  color: #fff;
  display: block;
  font-family: "lato", sans-serif;
  font-size: 24px;
  font-weight: 700;
  margin: 15px 0;
`;

export const SelectUseCurrency = styled.select`
  width: 100%;
  font-size: 18px;
  padding: 14px;
  border-radius: 10px;
`;
